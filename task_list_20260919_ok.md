## 任务列表处理

### 任务1. 完善任务规则（FINISHED）

- [x] 1. 完善任务规则
  - 状态：FINISHED
  - 需求：执行任务时，在每条任务下生成详细方案并给出开发步骤计划，可以分解为多条子任务；每次执行完一条，用 Markdown 的 todo 改为 done，所有子任务完成，则将父任务标记为 "FINISHED"。
  - 详细方案：在 `lib/core.js` 统一任务进度规则，要求执行 Agent 在父任务下补全实现方式、涉及文件或模块、依赖和验证方法，并按顺序执行、验证和勾选子任务。生成 Prompt 直接要求具体方案，本地生成与追加共用含方案和开发步骤的模板。仅成功后写入任务文件，失败时保留文件并输出错误；父任务须在所有子任务及验收通过后标记为 FINISHED。在 `server.js` 升级两版旧默认 Prompt，保留自定义 Prompt；调整父任务编号计算，排除嵌套步骤和围栏代码示例。
  - 开发步骤：
    - [x] 1. 更新执行和生成 Prompt，明确计划、子任务 todo/done、父任务 FINISHED 与失败时不修改文件的规则。
    - [x] 2. 统一本地任务生成与追加模板，为每条父任务提供详细方案和可细化的开发步骤。
    - [x] 3. 调整追加编号计算，忽略嵌套子任务和 Markdown 围栏示例，保留父任务的递增编号。
    - [x] 4. 兼容旧版默认 Prompt，保留用户自定义规则，并更新 README 的任务执行说明。
    - [x] 5. 补充核心与 API 回归覆盖，调整层级清单及 Windows 路径断言，完成语法检查和完整测试。
  - 执行记录：`npm run check` 通过；`npm test` 共 85 项，83 项通过、2 项因平台限制跳过、0 项失败；`git diff --check` 通过。

```markdown
- [x] 1. 已完成的任务
- [ ] 2. 未完成的任务
```

### 任务2. 如果所有的任务都完成了（FINISHED）

- [x] 2. 写入结束标志并停止运行循环
  - 状态：FINISHED
  - 需求：所有任务完成时，在任务文件最后添加结束标志，写入 "GGGG全部完成GGGG" 两遍，不要有间隔。每次执行 codex 或 claude 命令返回，都检查任务文件是否包含不间断的两遍标志；如果包含，则此任务全部完成，不再进入运行循环处理。
  - 完成标准：完成规则要求将连续双标志写入文件末尾；命令返回后即使没有输出标志、返回非零退出码或输出含有 429，也能按文件标志结束文本任务及循环。单个或有间隔的标志不误判；追加新任务后可继续执行。
  - 详细方案：在 `lib/core.js` 的执行、生成 Prompt 和本地模板中统一末尾结束标志规则，禁止未完成任务或规则示例提前包含完整双标志；保存上一版默认 Prompt，升级三版旧默认值并保留自定义配置。在 `server.js` 每轮命令返回后读取文件，文件完成标志优先于重试，复用完成状态、日志结束和目录队列释放；缺失或不可读文件继续按原输出规则处理。追加任务时移除旧标志并完整写回，保留已有任务及记录。同步 PowerShell、Bash 循环并更新 README；不增加外部依赖，通过核心、API 和模拟命令脚本测试验证。
  - 开发步骤：
    - [x] 1. 更新默认执行与生成规则、本地任务模板和旧版 Prompt 迁移，明确仅全部验证完成后在文件末尾写入连续双标志。
    - [x] 2. 增加命令返回后的任务文件检查，完成后停止调度、结束日志并释放运行器，保持媒体产物验收规则。
    - [x] 3. 追加新任务时清除旧结束标志，保留历史任务、方案、执行记录及递增编号。
    - [x] 4. 同步 PowerShell 与 Bash 的文件优先检查及连续双标志输出判定，更新 README 行为说明。
    - [x] 5. 验证文件完成、无输出、429、非零退出码、不连续标志、文件缺失或不可读、媒体验收、追加和 Prompt 迁移，完成语法检查与完整测试。
  - 执行记录：`npm run check` 通过；`npm test` 共 111 项，109 项通过、2 项因平台限制跳过、0 项失败；`bash -n claude_loop.sh`（Git Bash）和 PowerShell AST 语法检查通过；`git diff --check` 通过。全部实际父任务及子任务已完成，上方围栏中的未完成清单仅为 Markdown 示例。

GGGG全部完成GGGGGGGG全部完成GGGG
