# 多Agent任务台优化

生成日期：2026-07-25

## 任务信息

- 任务类型：text
- 工作目录：`/home/tanghama/project/tanghama_aol/claude_loop`
- 最终产物目录：未指定，直接更新现有项目文件
- 建议产物文件：未指定
- 输出格式：未指定
- 画面比例：不适用
- 分辨率：不适用
- 视频时长：不适用
- 参考文件：无

## 原始需求

1. 优化任务运行日志格式，使其呈现方式接近 VS Code 中 Claude Code 对话所展示的工作流程。
2. 将服务默认侦听 IP 从 `127.0.0.1` 改为 `0.0.0.0`。
3. 日志自动保存到文件；能够打开历史任务查看完整日志过程；能够在历史任务中追加新的任务项。

## 实施约束

- 保持项目零外部依赖，继续使用 Node.js 内置能力、CommonJS 和四空格缩进。
- 运行时日志及状态数据继续存放在已忽略版本控制的 `.claude-loop-data/` 下，不提交用户日志、环境变量或其他敏感数据。
- 保留现有任务、Profile、重试和进程控制能力；日志改造不得吞掉 `stdout`、`stderr`、退出码或错误信息。
- 新增或变更 API 时保持路径和返回结构语义清晰，并防止通过任务 ID、文件名或路径参数越权读取任意本地文件。
- 每完成一个任务项，应同步更新本文件中对应的状态和执行记录；不得删除仍有效的原始需求或既有历史记录。

## 总体验收标准

- 实时日志以清晰的对话/工作流形式按时间顺序展示，用户能辨认任务阶段、Agent/Profile、普通输出、错误、重试或 Profile 切换以及最终结果。
- 每次任务运行的日志均自动、增量地写入独立且可追溯的文件；服务重启后仍能从历史任务中打开并查看此前的完整过程。
- 用户可以向已结束的历史任务追加一个或多个新任务项；旧任务项、原始需求、既有日志和执行记录保持不变，新增项可继续进入正常执行流程。
- 服务默认绑定 `0.0.0.0`，同时保留通过环境变量显式覆盖监听地址和端口的能力。
- `npm run check` 与 `npm test` 全部通过，且测试过程不监听真实网络端口。

## 任务列表

- [x] 1. 统一后端任务运行日志的事件模型与采集流程
  - 状态：已完成
  - 工作内容：梳理 `server.js` 中任务启动、Prompt、Agent/Profile 选择、进程输出、重试、Profile 切换、停止和完成等日志来源，统一转换为可供实时展示和持久化复用的结构化事件；保留原始文本和多行内容，避免流式输出被重复、乱序或丢失。
  - 完成标准：
    - 每条事件至少可追溯到任务、单次运行、时间、事件类型及文本内容；涉及 Agent/Profile 的事件能显示对应名称。
    - 明确覆盖任务开始、真实 Prompt/命令信息、`stdout`、`stderr`、重试等待、Profile 切换、用户停止、进程退出及任务最终状态。
    - 实时日志接口能够按稳定顺序输出该模型，现有任务循环、重试和停止行为不发生回归。
    - 为事件生成、输出分段或状态映射等可独立验证的逻辑补充自动化测试。
  - 执行记录：2026-07-25 完成。新增带稳定序号、任务 ID、运行 ID、阶段、事件类型、原始文本、流类型、Profile 和元数据的结构化日志事件模型，并以 JSONL 与原纯文本日志同步增量写入；统一覆盖任务启动/预约、Agent 与 Profile 激活、真实命令和 Prompt、进程启动/首次输出/退出、stdout、stderr、错误、超时、重试等待、Profile 切换、用户停止、单轮完成及全部完成。`GET /api/tasks/:id/log` 现按稳定顺序返回 `events`，并支持 `?after=<sequence>` 增量读取，同时保留兼容的 `content`。补充核心事件解析测试及 API 生命周期、原始多行输出、游标、停止和 Profile 切换测试；`npm run check`、`npm test`（42 项，41 通过、1 项按平台跳过）和 `bash -n claude_loop.sh` 均通过。

- [x] 2. 实现运行日志的自动增量保存与历史读取
  - 状态：已完成
  - 工作内容：在 `.claude-loop-data/` 中为任务及其每次运行建立稳定的日志文件关联，在事件产生时增量写入，并提供安全的历史日志读取能力；兼顾现有数据或旧格式日志的兼容显示。
  - 完成标准：
    - 任务一开始即创建或关联日志文件，运行过程中持续落盘，而不是仅在正常完成后一次性保存。
    - 正常完成、失败、重试、用户停止和 Agent 进程异常退出时，已产生的日志均保留下来，并记录可理解的最终状态。
    - 同一任务的多次运行可区分且可按时间回看，不同任务的日志不会串写；服务重启后仍能根据任务记录定位日志。
    - 历史日志读取 API 对不存在、损坏和旧格式文件有明确且稳定的响应，不允许路径穿越或任意文件读取。
    - `test/core.test.js` 或 `test/server.test.js` 覆盖增量保存、重启后读取、多次运行隔离、异常日志和非法路径等关键场景。
  - 执行记录：2026-07-25 完成。为每个任务保留兼容的聚合 JSONL/纯文本日志，并在每次启动或生成运行时立即创建独立的运行日志与事件日志文件；所有结构化事件、stdout/stderr、错误、重试、停止和完成状态均同步增量写入，运行元数据持久化到任务记录并支持多次运行按时间区分。新增 `/api/tasks/:id/logs`、`/api/tasks/:id/logs/:runId` 及 `/api/tasks/:id/log?runId=` 历史读取能力，支持游标、运行列表、旧版纯文本、缺失文件和损坏 JSONL 的明确状态/警告；日志文件名、运行 ID 和读取路径均限制在 `.claude-loop-data/logs/` 内，并拒绝路径穿越及符号链接越界读取。补充增量落盘、服务重启读取、多次运行隔离、预约停止、异常/损坏/旧格式日志和非法路径测试；`npm run check`、`npm test`（44 项，43 通过、1 项按平台跳过）及 `bash -n claude_loop.sh` 全部通过。

- [x] 3. 支持在历史任务中追加任务项并恢复可执行状态
  - 状态：已完成
  - 工作内容：为已结束的历史任务增加追加任务项的后端能力，复用现有 Markdown 任务项格式和状态规则，并妥善处理完成态、失败态及并发运行状态。
  - 完成标准：
    - API 可向指定历史任务追加一个或多个非空任务项，并返回更新后的任务详情；输入校验失败时返回明确的 4xx 错误。
    - 新任务项按原有顺序之后写入，具有稳定编号或标识，初始状态为 `not_started`/未开始，不覆盖或重排旧任务项。
    - 原始需求、旧任务项、旧日志和旧执行记录完整保留；追加操作本身有时间及内容记录，刷新页面或重启服务后仍存在。
    - 对正在运行的任务采用明确且安全的策略（例如拒绝并返回冲突状态），避免并发写坏任务文件或运行状态。
    - 已处于 `all_done` 等结束状态的任务追加成功后，会恢复为可继续执行新增项的合理状态，同时已完成项不会被重复执行。
    - API 测试覆盖成功追加、批量追加、非法输入、任务不存在、运行中冲突、完成任务恢复及持久化场景。
  - 执行记录：2026-07-25 完成。新增历史任务任务项追加 API（规范路径为 `POST /api/tasks/:id/items`，并兼容 append/task-items 等语义别名），支持单项、批量项及带完成标准的对象输入；追加内容严格校验非空、长度和数量，按既有 Markdown 清单最大编号顺序追加 `not_started` 任务块，不改写原始需求、旧任务项、旧日志或执行记录。追加前校验任务运行状态、工作目录、目标文件和日志路径，运行中/预约/重试任务返回 409，非法输入返回 400，不存在任务返回 404；对 `all_done`、失败、停止等结束态清空下一次预约并重置循环停滞状态为可执行的 `not_started`，保留旧运行 ID 和历史元数据。追加记录同时写入任务状态的 `appendedItems`/`appendHistory`、事件流和兼容纯文本日志，服务重启后可恢复；补充 Markdown 核心函数及 API 的批量追加、非法输入、任务不存在、运行冲突、完成态恢复和重启持久化测试。`npm run check`、`npm test`（46 项，45 通过、1 项按平台跳过）、`bash -n claude_loop.sh` 和 `git diff --check` 均通过。

- [x] 4. 将实时与历史日志界面改造成对话式工作流视图
  - 状态：已完成
  - 工作内容：更新 `public/index.html`、`public/styles.css` 和 `public/app.js`，让实时任务和历史任务复用一致的日志呈现组件，以接近 VS Code 中 Claude Code 对话流程的方式展示结构化事件，而不是只显示难以阅读的连续原始文本。
  - 完成标准：
    - 日志按时间顺序显示为层次清晰的事件块，至少区分系统/阶段状态、Agent 输出、命令或 Prompt、`stderr`/错误、重试/Profile 切换和完成结果。
    - 每个事件块按需显示时间、Agent/Profile、状态或事件类型；多行文本、长命令和连续输出可读且不会破坏页面布局。
    - 实时运行时可增量追加并自动跟随最新内容；用户主动向上滚动查看旧内容时不会被强制拉回底部，并可手动恢复跟随。
    - 打开历史任务时从持久化日志加载完整过程，并能在同一历史任务界面输入和追加新任务项；成功后列表及任务状态立即刷新，失败时展示可理解的错误。
    - 空日志、加载中、读取失败、历史旧格式以及大量日志均有明确表现；界面在常见桌面和窄屏宽度下可用。
  - 执行记录：2026-07-25 完成。将运行页改造成深色终端工作舱，实时与历史日志共用结构化事件时间线：按阶段、Agent 输出、命令/Prompt、stderr/错误、重试/Profile 切换、停止和完成结果分类着色，显示时间、序号、运行阶段、Profile/模型和关键元数据；连续 stdout/stderr 块自动合并，多行 Prompt、长命令和大段输出使用可展开详情并安全转义。`loadLog` 使用 `after` 游标增量读取并合并事件，历史运行下拉框可按 runId 回看独立过程；新增加载中、空日志、读取失败、旧版纯文本及损坏日志提示。日志面板支持滚动时自动暂停跟随、手动恢复跟随，并加入响应式窄屏布局。历史任务运行页新增逐行/批量追加任务项表单，复用 `POST /api/tasks/:id/items`，成功后刷新任务状态、文件和日志，失败显示可理解错误；追加事件显示具体条目内容。使用临时数据目录通过 `agent-browser` 验证桌面 1440px 与窄屏 375px、实时游标更新、历史运行切换、向上滚动保持位置、跟随恢复、错误输出/多行 Unicode 和完成事件展示，测试数据已清理。`npm run check`、`npm test`（46 项，45 通过、1 项按平台跳过）、`bash -n claude_loop.sh` 和 `git diff --check` 均通过。

- [x] 5. 将服务默认监听地址改为 `0.0.0.0`
  - 状态：已完成
  - 工作内容：调整服务启动配置，使默认监听地址从 `127.0.0.1` 变为 `0.0.0.0`，并保留显式配置能力及准确的启动提示。
  - 完成标准：
    - 未设置监听地址环境变量时，`npm start` 使用 `0.0.0.0`；设置合法的 `HOST`（或项目既有等价配置）后使用用户指定地址。
    - `PORT` 自定义行为保持不变，启动日志清楚说明实际绑定地址和端口，并提示远程访问仍受本机网络及防火墙限制。
    - 相关配置解析具有自动化测试，测试使用 `server.inject()` 或可测试的纯函数，不监听真实端口。
    - README 中的启动和访问说明与新默认值一致，不将 `0.0.0.0` 错写成浏览器必须访问的目标地址。
  - 执行记录：2026-07-25 完成。新增可测试的服务监听配置解析，默认 `HOST` 为 `0.0.0.0`，保留 `HOST`/`PORT` 显式覆盖及 `PORT=0` 行为；启动提示区分实际绑定地址与浏览器本机访问地址，并明确说明远程访问受本机网络和防火墙设置限制。README 补充默认绑定、局域网访问注意事项及 Bash/PowerShell 配置示例；新增配置和启动提示自动化测试，未监听真实端口。`npm run check`、`npm test`（47 项，46 通过、1 项按平台跳过）、`bash -n claude_loop.sh` 和 `git diff --check` 均通过。

- [x] 6. 完成回归测试、文档与端到端验收
  - 状态：已完成
  - 工作内容：补齐跨模块测试和使用说明，验证结构化实时日志、文件持久化、历史查看、追加任务及监听地址变更能够协同工作。
  - 完成标准：
    - 自动化测试至少覆盖一次完整任务生命周期：创建/启动任务、产生多类日志、完成或失败、重新加载历史日志、追加新任务项并确认仅新增项待执行。
    - 验证日志中包含特殊字符、Unicode、多行内容和较大输出时仍可正确保存、读取和显示，且不会造成前端脚本注入。
    - README 说明日志文件位置、历史日志查看方式、历史任务追加方式、默认监听地址及可用环境变量。
    - 运行 `npm run check`、`npm test` 和 `bash -n claude_loop.sh` 均通过；测试创建的临时状态和日志得到清理，不污染仓库。
    - 人工验收记录包含实时日志视图、服务重启后的历史查看、已完成任务追加新项、Profile 切换/错误事件展示以及局域网访问配置结果。
  - 执行记录：2026-07-25 完成。新增 `test/server.test.js` 完整生命周期回归：从现有 Markdown 创建任务并启动 Agent，验证命令/Prompt、stdout、stderr、进程退出和完成事件，保存并重启读取同一 `runId` 的完整日志，向 `all_done` 历史任务追加带完成标准的新项，重启后确认旧项保持原样且只有新增项为 `not_started`，再次运行后确认新增项状态可继续推进；同用例覆盖 HTML 片段、Unicode、多行和 16 KB 级输出。新增 `test/frontend.test.js`，执行实际日志渲染器的转义逻辑并断言 `<script>` 不会被插入为 DOM 节点；既有回归继续覆盖 429 错误、Profile 切换、旧格式/损坏日志、路径越权和停止场景。README 新增日志目录、聚合/单次运行文件、历史读取 API、`after` 游标、追加 API、默认监听地址和 `HOST`/`PORT` 配置说明，并记录本次人工验收。使用临时数据目录通过 `agent-browser` 验证 1440×900 实时日志时间线、服务重启后的历史运行下拉、已完成任务追加反馈、错误输出/Profile 切换和 375×812 窄屏布局；HTML/脚本内容仅作为文本显示，临时服务、浏览器会话和数据均已清理。`npm run check`、`npm test`（49 项，48 通过、1 项按平台跳过）、`bash -n claude_loop.sh` 和 `git diff --check` 全部通过；监听配置测试确认默认绑定 `0.0.0.0`、可用 `HOST`/`PORT` 覆盖及局域网访问提示。

- [ ] 7. # 问题修改
  - 状态：未开始
  - 完成标准：实现并验证该任务项，必要时更新相关文件。
  - 执行记录：暂无

- [x] 8. 从日志看，项目实际并未全部完成，但是任务就完成了，请检查并修复问题，原条件是在结果中输出了"GGGG全部完成GGGG"两遍，不要有间隔才算完成，但是现在好像没满足条件就说完成了
  - 状态：已完成
  - 完成标准：实现并验证该任务项，必要时更新相关文件。
  - 执行记录：2026-08-01 完成。定位到后端 `isAllDoneOutput` 同时接受连续双标记、单个 `GGGG全部完成GGGG` 和旧版完成文本，导致单标记或提示词回显可能误判为 `all_done`。将判定提取为 `lib/core.js` 中的可测试函数，并收紧为仅当输出包含连续无间隔的 `GGGG全部完成GGGGGGGG全部完成GGGG` 时才判定全部完成；移除旧标记兼容判定，README 同步明确退出条件。新增核心测试覆盖双标记、单标记、换行/空格分隔和旧标记，并新增服务回归验证单标记只进入重试等待且不产生 `task_all_done`；现有需要正常结束的测试 Agent 均改为输出连续双标记。`npm run check`、`npm test`（63 项，62 通过、1 项按平台跳过）、`bash -n claude_loop.sh` 和 `git diff --check` 全部通过。

- [ ] 9. 运行菜单输出尾部的滚动条无法拖到最底，会自动回滚到上面
  - 状态：未开始
  - 完成标准：实现并验证该任务项，必要时更新相关文件。
  - 执行记录：暂无
